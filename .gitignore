node_modules/
__pycache__/
.DS_Store
*.pyc
*.zip

# Thomas's unpublished specification. Present locally so `build.py
# --with-source` works for a private copy; never committed, because this
# repository is public and publishing his work is his call, not ours.
sources/

# Working notes never belong in a public repo. Handoff briefs, scratch plans
# and session state live in GRIP, not here.
plans/
notes/
drafts/
*.WORKLOG-*.md
